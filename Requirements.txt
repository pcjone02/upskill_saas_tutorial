Memberships
* User should see link for Basic plan on home page
* User should see link for Pro plan on home page
* Basic plan link should point to regual Devise sign up form
* Pro plan link should point to a custom sign up form

Stripe Integration
* Users should have a stripe customer token in the users table.
* Java should prevent the Prom form from submitting, when user fills it out, and shoudld send card info to Stripe.  Stripe will retunr with a card token.
* Java should send user fields and card token to oiur rails app. Rails app will validate the the user fields. Rails app will note whether the plan 1 or plan 2 is in play.
* Once validated rails should send user info (email, plan id) and card token to Stripe server and Stripe will create a subscription and send back a customer token. Rails should save customer token and user infomraiton to our database.
